<% include partials/_header %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-4">
      <div class="card-body">
         <h5 class="card-title">Proyecto:</h5>
        <form action="/add" method="POST">
          <div class="form-group">
            <input type="text" name="name" placeholder="Nombre" class="form-control" autofocus maxlength="40" required>
          </div>
          <div class="form-group">
            <input type="text" name="address" placeholder="Descripción" class="form-control" maxlength="90" required>
          </div>
          <div>
            <button type="submit" class="btn btn-primary">
              Guardar proyecto
            </button>
              <a href="/report" class="btn btn-primary">Reporte</a>
          </div>
        </form>
      </div>
    </div>
    <div class="col-md-8">
        <div class="card-body">
           <h5 class="card-title">Tareas:</h5>
          <table class="table table-bordered table-hover">
            <thead>
              <tr>
                <td>Colaborador</td>
                <% if (data) { %>
                  <% for(var i = 0; i < data.length; i++) { %>
                      <td><%= data[i].nombre %>

                        <a href="/update/<%= data[i].id %>" class="glyphicon glyphicon-pencil"></a>
                        <a href="/delete/<%= data[i].id %>" class="glyphicon glyphicon-trash"></a>
                      </td>
                  <% } %>
                <% } %>
                <td>
                    <a href="/add/" class="glyphicon glyphicon-plus"></a>
                </td>
              </tr>
            </thead>
            <tbody><!-- Falta hacer loop para llenar datos-->
              <tr>
                <td>Daniel</td>
              </tr>
              <tr>
                <td>Jose</td>
              </tr>
              <tr>
                <td><a href="/addRecurso/" class="glyphicon glyphicon-plus"></a></td>
              </tr>
            <!--  <% if (data) { %>
                <% for(var i = 0; i < data.length; i++) { %>
                  <tr>
                    <td><%= data[i].name %></td>
                  </tr>
                <% } %>
              <% } %> -->
            </tbody>
          </table>
      </div>
    </div>
  </div>
</div>

<% include partials/_footer %>
